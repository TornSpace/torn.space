name: CI
on:
  push:
    branches:
      - master
    paths: &paths
      - "api/**"
      - "shared/**"
      - "*.json"
      - "bun.lock"
      - ".github/workflows/api.yml"
  pull_request:
    paths: *paths
jobs:
  api:
    name: API
    runs-on: ubuntu-latest
    concurrency: ci-api-${{ github.ref }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install Packages
        run: bun ci --filter api --filter shared

      - name: Typecheck API
        run: bun typecheck
        working-directory: ./api

      - name: Build API
        run: bun run build
        working-directory: ./api
