{
  "name": "torn.space",
  "version": "4.0.0-beta",
  "private": true,
  "description": "Community-led torn.space clone.",
  "license": "AGPL-3.0-only",
  "author": "DamienVesper",
  "workspaces": [
    "api",
    "bot",
    "client",
    "common",
    "server"
  ],
  "type": "module",
  "scripts": {
    "fmt": "bun --bun oxfmt",
    "fmt:ci": "oxfmt --check",
    "lint": "oxlint",
    "lint:ci": "oxlint --format=github",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {
    "hjson": "^3.2.2"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/hjson": "^2.4.6",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
