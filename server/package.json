{
  "name": "@torn.space/server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir=build --target bun --production",
    "dev": "bun --watch src/index.ts",
    "start": "bun build/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
